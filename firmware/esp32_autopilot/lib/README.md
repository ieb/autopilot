# Local Fake Libraries

In this directory are fake libraries to allow the simulator to operate on macOS. They are copies of the real dependencies patched to fake the real ESP32 hardware an allow values from the physical hardware to be injected.

Do not try and use these librararies for real hardware, they will not work.